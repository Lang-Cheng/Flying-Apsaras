import React, { FC } from 'react';

import { Container } from '@material-ui/core';

const Products: FC = () => {
    return <Container maxWidth="lg">开发中...敬请期待</Container>;
};

export default Products;
